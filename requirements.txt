torch>=2.1
transformers>=5.5
peft>=0.18
accelerate
flask
pillow
opencv-python
openai
requests
pandas
huggingface_hub

# tools/ocr_proxy.py and tools/resize_for_ocr.py only need flask, requests, pillow
# (already listed above) — no extra dependencies
